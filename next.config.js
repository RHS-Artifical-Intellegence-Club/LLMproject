/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Explicitly set output mode for consistent behavior
  output: 'standalone',
  // Removed deprecated options
};

module.exports = nextConfig;
